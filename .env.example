# 프론트 환경변수 예시 (이 파일은 git에 commit, 실제 값은 .env.local에)

# 백엔드 API 베이스 URL
VITE_API_URL=http://localhost:8080

# Mock Service Worker 활성화 (true일 때 mock API 사용, false일 때 실 백엔드)
VITE_USE_MOCK=true

# 네이버 로그인 Client ID (Secret은 백엔드만, 프론트엔 ID만)
VITE_NAVER_CLIENT_ID=__네이버_개발자센터에서_발급받은_Client_ID__
