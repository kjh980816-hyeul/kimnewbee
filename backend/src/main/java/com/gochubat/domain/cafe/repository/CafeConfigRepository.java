package com.gochubat.domain.cafe.repository;

import com.gochubat.domain.cafe.entity.CafeConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CafeConfigRepository extends JpaRepository<CafeConfig, Long> {
}
